from core.derived import and, exists, eqv

# exists_unique(x, body_fn): exists x. body(x) and forall y. body(y) -> eqv(x, y)
$exists_unique \\(x body_fn : exists(x, and(body_fn(x), forall(y, implies(body_fn(y), eqv(x, y))))))
