from core.derived import mem, or, eqv
from vocab.sets import successor
from theorems.sets.set_spec_unique import set_spec_unique

# |- successor(s1,a) -> successor(s2,a) -> eqv(s1,s2)
$successor_unique set_spec_unique(\v: or(mem(v, "a"), eqv(v, "a", "__")), ["a"])
