from derived import mem, and, sep_ax
from tactics import ax

# Separation |- forall b a. exists s. forall x. iff(mem(x,s), and(mem(x,a), mem(x,b)))
$_sep sep_ax(["b"], \x a p: mem(x, "b"), "_")
$intersect_exists ax([_sep], [_sep], _sep)
