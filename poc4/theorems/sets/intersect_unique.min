from derived import mem, and, eq
from vocab.sets import intersect
from theorems.sets.set_spec_unique import set_spec_unique

# |- intersect(s1,a,b) -> intersect(s2,a,b) -> eq(s1,s2)
$intersect_unique set_spec_unique(\v p: and(mem(v, "a"), mem(v, "b")), ["b", "a"], "_")
