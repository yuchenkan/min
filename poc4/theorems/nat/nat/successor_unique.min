from derived import mem, or, eq
from vocab.nat.nat import successor
from theorems.sets.set_spec_unique import set_spec_unique

# |- successor(s1,a) -> successor(s2,a) -> eq(s1,s2)
$successor_unique set_spec_unique(\v p: or(mem(v, "a"), eq(v, "a", p)), ["a"], "_")
