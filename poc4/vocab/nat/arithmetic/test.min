from derived import show
from vocab.nat.arithmetic.plus import plus
from vocab.nat.arithmetic.mult import mult, add_func, mult_func

$_ tap(show(plus("m", "n", "p", "_")))
$_ tap("plus ok")

$_ tap(show(mult("m", "n", "r", "_")))
$_ tap("mult ok")

$_ tap(show(add_func("af", "m", "_")))
$_ tap("add_func ok")

$_ tap(show(mult_func("h", "_")))
$_ tap("mult_func ok")

$ok true
